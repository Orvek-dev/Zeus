services:
  zeus:
    image: zeus-agent:1.0.0-alpha.1
    build:
      context: .
      dockerfile: Dockerfile
    container_name: zeus-agent
    command: ["sleep", "infinity"]
    environment:
      PYTHONUNBUFFERED: "1"
    volumes:
      - zeus_home:/home/zeus/.zeus

volumes:
  zeus_home:
