# Security Policy

Zeus is governed and local-first by default.

## Defaults

- No remote telemetry is enabled by the package.
- Local runtime state should stay outside source control.
- Provider credentials should be referenced by environment variable or external secret manager name, never copied into configs, tests, logs, or evidence.
- Secret-looking values are redacted in runtime surfaces that handle credential-like text.
- Tool, connector, transport, gateway, and live provider paths should pass through authority, lease, and evidence boundaries before execution.
- Dry-run and live-capable paths should remain distinguishable in code and review.

## Current v1.6.0 Boundary

`v1.6.0` is the sixth post-stable governed live platform expansion. It includes deterministic local scenarios, provider and tool interfaces, native tool catalog reporting, MCP discovery and API connector dry-run contracts, CLI/API/gateway/ACP/batch/Python-library entrypoint contracts, local MemoryGraph storage, LLM Wiki views, ontology review queues, skill-learning memory bridges, retention-policy reporting, adaptive ULW workflow selection, critique checkpoints, live readiness, local opt-in smoke, live cockpit reporting, live beta provider/MCP/gateway contracts, production foundation reporting for identity/auth, approval, runtime lease, credential binding, secret resolver, audit, sandbox, rollback, and independent-review controls, provider live API status reporting, loopback-only provider smoke execution, MCP live-server status reporting, MCP activation policy, request envelope, loopback HTTP smoke execution, prompt-injection scanning, gateway live-delivery status reporting, configured target allowlist, pairing proof, delivery envelope, delivery body, loopback gateway transport, loopback gateway HTTP delivery, Sandbox Terminal Live reporting, Memory Privacy Live reporting for local SQLite MemoryGraph schema readiness, explicit local fact smoke, secret quarantine, retention deletion, cross-session search default-deny, no auto-promotion posture, Provider Live Opt-in reporting, Provider Owned Client Live reporting, MCP Owned Client Live reporting, Stable Release reporting, Real Provider Runtime reporting, Real MCP Runtime reporting, Real Platform Runtime reporting, Real Execution Runtime reporting, Real Memory Operation Runtime reporting, and Real Self Evolution Runtime reporting for eval-learning smoke, reviewable skill proposals, workflow critique memory, promotion block, secret-boundary checks, cleanup, and no-production-claim posture. Provider Runtime status commands do not open network access or read credential material. Provider Runtime local deterministic smoke does not open network access or read credential material. Provider Runtime external receipt smoke requires a scoped environment secret reference and binds remote policy, preflight, receipt, audit, and redaction before reporting readiness, but it still does not claim unrestricted or production provider execution. MCP Runtime status, setup, list, inspect, and login dry-run commands do not open network access, start subprocesses, or read credential material. MCP Runtime governed test uses a fake client and cleanup receipt, not a production remote MCP server. Platform Runtime status and API dry-run commands do not bind sockets, start hosted servers, or execute live handlers. Platform Runtime gateway/session smoke uses temporary local stores and cleanup receipts. Execution Runtime local smoke uses controlled local sandbox execution through lease, approval, evidence, sandbox policy, cleanup, and no-secret-echo checks. Memory Operation Runtime writes only local reviewable facts, quarantine/delete records, and skill-learning bridge facts. Self Evolution Runtime turns verified eval/workflow evidence into reviewable candidates and local critique facts only; it does not auto-promote active skills, active rules, workflow patterns, memory, ontology, wiki pages, authority, or production live state. MCP resources/prompts remain disabled by default, production remote MCP server execution remains blocked, external gateway delivery remains blocked, webhook production execution remains disabled, browser live navigation remains blocked, Docker/SSH/remote sandbox execution remains blocked, cross-session memory search remains denied by default, and network egress remains denied by default outside explicitly leased provider/MCP/gateway receipt scenarios. This release does not claim production external provider execution, remote MCP production execution, hosted API daemon readiness, external gateway/webhook production execution, browser live execution, remote sandbox execution, unrestricted production live readiness, unattended execution, self-modifying workflows, automatic memory writes from ordinary agent turns, ontology promotion, learned-rule promotion, active rule writes, active skill writes, workflow pattern promotion, or hard-isolated remote execution by itself.

Before production use, live MCP tools, external AI APIs, browser/terminal automation, cron workers, remote sandboxes, networked gateways, and memory/ontology promotion paths should be wired through explicit authority grants, runtime leases, credential-scope binding, retention policy, human approval when needed, sandbox egress policy, audit evidence, and rollback behavior.

See [docs/live-connection-architecture.md](docs/live-connection-architecture.md) for the target live connection design.

## Reporting

Please report security issues privately before public disclosure.
