# Security Policy

Zeus is governed and local-first by default.

## Defaults

- No remote telemetry is enabled by the package.
- Local runtime state should stay outside source control.
- Provider credentials should be referenced by environment variable or external secret manager name, never copied into configs, tests, logs, or evidence.
- Secret-looking values are redacted in runtime surfaces that handle credential-like text.
- Tool, connector, transport, gateway, and live provider paths should pass through authority, lease, and evidence boundaries before execution.
- Dry-run and live-capable paths should remain distinguishable in code and review.

## Current v1.0.0-rc.9 Boundary

`v1.0.0-rc.9` is a governed MCP Owned Client Live source checkpoint. It includes deterministic local scenarios, provider and tool interfaces, native tool catalog reporting, MCP discovery and API connector dry-run contracts, CLI/API/gateway/ACP/batch/Python-library entrypoint contracts, local MemoryGraph storage, LLM Wiki views, ontology review queues, skill-learning memory bridges, retention-policy reporting, adaptive ULW workflow selection, critique checkpoints, live readiness, local opt-in smoke, live cockpit reporting, live beta provider/MCP/gateway contracts, production foundation reporting for identity/auth, approval, runtime lease, credential binding, secret resolver, audit, sandbox, rollback, and independent-review controls, provider live API status reporting, loopback-only provider smoke execution, MCP live-server status reporting, MCP activation policy, request envelope, loopback HTTP smoke execution, prompt-injection scanning, gateway live-delivery status reporting, configured target allowlist, pairing proof, delivery envelope, delivery body, loopback gateway transport, loopback gateway HTTP delivery, Sandbox Terminal Live reporting, Memory Privacy Live reporting for local SQLite MemoryGraph schema readiness, explicit local fact smoke, secret quarantine, retention deletion, cross-session search default-deny, no auto-promotion posture, Provider Live Opt-in reporting for explicit operator opt-in, endpoint allowlisting, scoped secret references, remote transport policy, remote executor preflight, external provider receipt validation, audit, response redaction, and no-production-claim posture, Provider Owned Client Live reporting for owned client adapter execution through remote policy, credential handoff, preflight, provider receipt validation, audit, redaction, cleanup, and no-production-claim posture, and MCP Owned Client Live reporting for remote MCP tool execution through an owned client adapter with explicit operator opt-in, endpoint allowlisting, scoped secret reference checks, remote MCP policy, credential handoff, preflight, owned client receipt validation, audit, redaction, cleanup, resources/prompts disabled posture, and no-production-claim posture. Memory/Ontology and Memory Privacy status commands may initialize the local SQLite MemoryGraph schema under the selected Zeus home. Adaptive Zeus status commands only plan dry-run workflow patterns. Live Beta Candidate and Production Foundation status commands only compose readiness and control evidence. Provider Live API, MCP Live Server, Gateway Live Delivery, Provider Live Opt-in, Provider Owned Client Live, and MCP Owned Client Live status commands do not open network access. Provider, MCP, and gateway loopback smoke commands open only a session-local loopback server after the operator supplies a scoped environment secret reference, redact secret-bearing material, capture audit evidence, and clean up the smoke server before reporting readiness. Provider Live Opt-in `external-receipt-smoke`, Provider Owned Client Live `owned-client-smoke`, and MCP Owned Client Live `owned-client-smoke` require a scoped environment secret reference and bind remote policy, preflight, receipt, audit, and redaction before reporting readiness, but they still do not claim unrestricted or production provider or MCP execution. Sandbox Terminal Live `local-smoke` can run only allowlisted local commands inside a temporary sandbox through lease, approval, broker dispatch, safe environment, evidence capture, and cleanup. Memory Privacy Live `secret-quarantine`, `delete-retention`, and `promotion-block` scenarios prove secret-like memory is quarantined and scrubbed, deleted facts leave the active snapshot, and active-rule or ontology promotion remains blocked. MCP resources/prompts remain disabled by default, production remote MCP server execution remains blocked, unsafe MCP tool descriptions are blocked by the prompt-injection scan, external gateway delivery remains blocked, webhook production execution remains disabled, browser live navigation remains blocked, Docker/SSH/remote sandbox execution remains blocked, cross-session memory search remains denied by default, and network egress remains denied by default outside the explicit Provider Live Opt-in, Provider Owned Client Live, and MCP Owned Client Live receipt scenarios. This release candidate does not claim production external provider execution, remote MCP production execution, external gateway/webhook production execution, browser live execution, remote sandbox execution, production live readiness, unattended execution, self-modifying workflows, automatic memory writes from ordinary agent turns, ontology promotion, learned-rule promotion, active rule writes, or hard-isolated remote execution by itself.

Before production use, live MCP tools, external AI APIs, browser/terminal automation, cron workers, remote sandboxes, networked gateways, and memory/ontology promotion paths should be wired through explicit authority grants, runtime leases, credential-scope binding, retention policy, human approval when needed, sandbox egress policy, audit evidence, and rollback behavior.

See [docs/live-connection-architecture.md](docs/live-connection-architecture.md) for the target live connection design.

## Reporting

Please report security issues privately before public disclosure.
